# Result Metadata

This folder stores JSON outputs that describe generated results, feature sets, and run summaries.

These files are useful for reproducibility and for checking exactly which feature sets or model comparisons produced a table or report.

Optional extension:

- `pytorch_neural_network_baseline_metrics.json`: metrics, configuration, and training history for the PyTorch feedforward neural-network baseline.
