# Optional PyTorch extension dependencies.
# Install the verified core environment first, then PyTorch:
#
#   python -m pip install -r requirements-pytorch.txt
#
# This file is intentionally separate from requirements.txt so the canonical
# scikit-learn workflow remains lightweight and reproducible.

-r requirements-verified.txt
torch==2.12.1
